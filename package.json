{
  "name": "ens-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify index.js -o bundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ethereum-ens": "^0.5.0",
    "web3": "^0.18.2"
  },
  "devDependencies": {
    "browserify": "^14.1.0"
  }
}
